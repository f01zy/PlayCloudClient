import Register from "@/page/Register";
import { Metadata } from "next";

export const metadata: Metadata = {
  title: "Register | PlayCloud"
}

const RegisterPage = () => {
  return <Register />
}

export default RegisterPage;